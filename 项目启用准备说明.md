# FastBug 项目启用准备说明

本文用于在一台新的 Windows 电脑上启用 FastBug：平板一键捕获现场证据、工作台整理草稿、上传 OSS，并同步到云效缺陷。

## 1. 启用范围

- `android-agent/`：安装到安卓平板的采集 Agent，负责悬浮窗触发与回放录屏。
- `collector/`：运行在 Windows 电脑的本地服务，默认地址为 `http://127.0.0.1:52741`。
- `collector-data/`：本机证据、草稿和运行配置目录；含业务数据与下载链接，默认不提交 Git。
- 云效：创建或更新缺陷工作项。
- OSS：保存完整证据 ZIP，并产生限时下载入口。

## 2. 必备依赖

| 项目 | 最低要求 | 用途 |
| --- | --- | --- |
| Windows | Windows 10/11 | 运行 Collector、ADB 和工作台 |
| Node.js | 18 或更高版本 | 运行 Collector；当前实现不依赖 npm 包 |
| Android Platform Tools | 含 `adb`，并已加入 `PATH` | USB 设备连接、拉取录屏和日志 |
| Android Studio 或 Gradle | Android Gradle Plugin 7.4.2、compileSdk 34 | 构建 `android-agent` |
| JDK | 11 或 17 | Android 构建环境 |
| 安卓平板 | Android 8.0（API 26）或更高 | 运行 Capture Agent |
| USB 数据线 | 可用 ADB 调试 | 平板与电脑的数据通道 |

检查基础环境：

```powershell
node --version
adb version
adb devices
```

`adb devices` 中目标设备必须显示为 `device`；若显示 `unauthorized`，请在平板上确认 USB 调试授权。

## 3. 第一次配置

### 3.1 获取设备序列号和被测包名

```powershell
adb devices
adb -s <设备序列号> shell getprop ro.product.model
```

Collector 的 `--package` 应填被测 App 包名。POC 验证 Capture Agent 自身时，可使用 `com.fastbug.captureagent`。

### 3.2 构建并安装平板 Agent

推荐在 Android Studio 中打开 `android-agent/`，等待 Gradle 同步后执行 Run（app）。也可以在已安装 Gradle 的终端执行：

```powershell
Set-Location android-agent
gradle assembleDebug
adb -s <设备序列号> install -r app\build\outputs\apk\debug\app-debug.apk
```

平板上首次打开 Agent 后，需要依次授权：

1. 悬浮窗权限；
2. 录屏 / 屏幕捕获权限；
3. 保持 USB 调试连接。

## 4. 云效配置

复制配置示例，填写企业与项目固定信息：

```powershell
Copy-Item collector\yunxiao.config.example.json collector-data\yunxiao.config.json
```

`collector-data/yunxiao.config.json` 的字段：

| 字段 | 说明 |
| --- | --- |
| `domain` | 云效开放 API 域名，中心版通常为 `openapi-rdc.aliyuncs.com` |
| `edition` | `center` 或 `region` |
| `organizationId` | 中心版企业 ID |
| `spaceId` | 云效项目空间 ID |
| `workitemTypeId` | 缺陷（Bug）工作项类型 ID |
| `assignedTo` | 可选默认负责人；工作台选择负责人时会覆盖它 |

将具有项目读写权限的个人访问令牌配置为 Windows 用户环境变量。不要把令牌写入 JSON、代码或 Git：

```powershell
[Environment]::SetEnvironmentVariable('YUNXIAO_TOKEN', '<你的云效令牌>', 'User')
```

## 5. OSS 配置

完整证据包上传到 `oss://zstt-test-tmp/fastbug/`，每条记录会形成：

```text
fastbug/<会话目录>/evidence.zip
```

配置以下 Windows 用户环境变量。`FASTBUG_OSS_ENDPOINT` 可以填写 Bucket 的外网 Endpoint；即使误填带 Bucket 前缀的 Endpoint，Collector 也会兼容处理。

```powershell
[Environment]::SetEnvironmentVariable('FASTBUG_OSS_ENDPOINT', '<Bucket 外网 Endpoint>', 'User')
[Environment]::SetEnvironmentVariable('FASTBUG_OSS_ACCESS_KEY_ID', '<RAM AccessKey ID>', 'User')
[Environment]::SetEnvironmentVariable('FASTBUG_OSS_ACCESS_KEY_SECRET', '<RAM AccessKey Secret>', 'User')
```

RAM 账号至少需要目标 Bucket、`fastbug/` 前缀下的对象读写权限。下载链接默认有效 7 天。AccessKey Secret 不可发送到聊天、写入配置文件或提交 Git。

## 6. 启动 Collector

新建 PowerShell 窗口后执行以下命令，将用户环境变量注入当前运行进程：

```powershell
$env:YUNXIAO_TOKEN = [Environment]::GetEnvironmentVariable('YUNXIAO_TOKEN', 'User')
$env:FASTBUG_OSS_ENDPOINT = [Environment]::GetEnvironmentVariable('FASTBUG_OSS_ENDPOINT', 'User')
$env:FASTBUG_OSS_ACCESS_KEY_ID = [Environment]::GetEnvironmentVariable('FASTBUG_OSS_ACCESS_KEY_ID', 'User')
$env:FASTBUG_OSS_ACCESS_KEY_SECRET = [Environment]::GetEnvironmentVariable('FASTBUG_OSS_ACCESS_KEY_SECRET', 'User')

node collector/index.js start --serial <设备序列号> --package <被测包名>
```

启动后 Collector 会：

1. 建立 `adb reverse tcp:52741 tcp:52741`；
2. 生成会话 ID；
3. 启动日志采集；
4. 在浏览器打开工作台：`http://127.0.0.1:52741/`。

将控制台输出的会话 ID 配置给 Agent，或使用以下命令启动 Agent 并自动传入本机地址和会话 ID：

```powershell
adb -s <设备序列号> shell am start -n com.fastbug.captureagent/.MainActivity --es collector_url http://127.0.0.1:52741 --es session_id <会话ID>
```

工作台顶部的“设备连接”卡片应显示“已连接”、设备信息与会话 ID。会话 ID 由 Collector 管理，日常不需要手动编辑。

## 7. 日常操作

1. 在平板 Agent 点击“开始采集”；
2. 测试中发生问题时点击悬浮窗“报缺陷”；
3. 工作台会先出现正在处理的会话，文件传输完成后显示草稿；
4. 在草稿中补充标题、预期结果、实际结果和复现步骤；
5. 用输入联想选择云效“应用”和“负责人”，保存本地草稿；
6. 点击“提交到云效”。系统会先上传完整证据包至 OSS，再创建或更新云效缺陷；
7. 云效“技术证据”区保留“下载完整证据包”入口，不在“实际结果”重复写入链接。

## 8. 验收清单

- [ ] 工作台设备连接状态为“已连接”；
- [ ] 平板报缺陷后，工作台立即显示会话并展示处理进度；
- [ ] 完成后可查看录屏、截图和日志；
- [ ] 选择应用、负责人后本地草稿可保存；
- [ ] 提交后 OSS 中出现 `evidence.zip`；
- [ ] 云效缺陷包含环境、复现步骤、预期结果、实际结果和技术证据下载入口；
- [ ] 本地 `collector-data/`、令牌和 OSS 密钥未被 Git 跟踪。

## 9. 常见问题

| 现象 | 优先检查 |
| --- | --- |
| 工作台显示“未连接” | USB 数据线、`adb devices` 是否为 `device`、Collector 是否仍在运行 |
| 平板无法上报 | `adb reverse --list`、Collector 地址为 `127.0.0.1:52741`、会话 ID 是否一致 |
| 云效选项加载失败 | `YUNXIAO_TOKEN` 是否存在、令牌是否有该项目读取权限、企业/项目 ID 是否正确 |
| OSS 上传失败 `NoSuchBucket` | Endpoint 是否来自目标 Bucket 的“概览 → Endpoint（外网访问）” |
| OSS 上传 `403` | RAM 权限、AccessKey、Bucket 与前缀权限是否正确 |
| 云效缺陷未创建 | 草稿是否已保存，是否选择了应用和负责人，云效令牌是否具备写入权限 |

